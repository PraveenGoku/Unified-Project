import { Routes } from '@angular/router';
import { IframeViewerComponent } from './iframe-viewer/iframe-viewer.component';

export const routes: Routes = [
  { path: '', component: IframeViewerComponent }

];

